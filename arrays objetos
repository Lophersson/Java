String[] palabras = {"Hola", "Mundo", "Java"};

// Buscar elemento
int pos = -1;
for (int i = 0; i < palabras.length; i++) {
    if (palabras[i].equals("Java")) {
        pos = i;
        break;
    }
}
